export type SkillType = {
    name: string,
    description: string
}

export type SkillEntity = {
    name: string,
    description: string,
    id: string
}

export type SkillId = {
    id: string
}